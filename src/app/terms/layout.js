export const metadata = {
  title: 'Foydalanish shartlari',
  description:
    'FATH savdo roboti foydalanish shartlari, maxfiylik siyosati va qaytarish qoidalari.',
};

export default function Layout({ children }) {
  return children;
}
